# =============================================================================
# NPM2PiHole Configuration - API-Based Domain Management
# =============================================================================

# Pi-hole Configuration
# Pi-hole host for remote SSH access
PIHOLE_HOST=192.168.1.217
# Target hostname for CNAME records (where NPM is running)
NPM_TARGET_HOST=npm.home.ryanmo.net

# NPM Configuration
# Domain suffix for all services
DOMAIN_SUFFIX=home.ryanmo.net
# NPM API endpoint (hostname:port)
NPM_HOST=npm-admin.home.ryanmo.net
# NPM admin email for API authentication
NPM_EMAIL=admin@example.com
# NPM admin password for API authentication
NPM_PASSWORD=changeme
# NPM certificate ID to use for all proxy hosts (check NPM admin for certificate IDs)
NPM_CERTIFICATE_ID=1

# System Configuration
# Testing mode - set to true to see what changes would be made without applying them
TESTING_MODE=false
# How often to check for changes (in seconds)
# Examples: 900 = 15 minutes, 3600 = 1 hour, 43200 = 12 hours
SLEEP_INTERVAL=900